import styles from "./index.module.scss";

export default function CardMovie({ data }) {
  console.log(data);
  return (
    <article className={`box`}>
      <h2>Movie single</h2>
    </article>
  );
}
